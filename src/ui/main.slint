import { CandidateData, CandidateWindow } from "candidate.slint";
import { StatusBar } from "status_bar.slint";
import { TrayMenu } from "tray_menu.slint";

export { CandidateData, CandidateWindow, StatusBar, TrayMenu }
